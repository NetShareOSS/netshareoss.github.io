# Paste the Cloudflare Worker download URL below (one line) AFTER you deploy
# cloudflare/download-worker (see that folder’s README). Do not use a public
# r2.dev URL here once the Worker is the gate — that bypasses rate limits.
#
# Worker URL only, not public r2.dev.
https://netshare-macos-dmg.netshareoss.workers.dev/releases/macos/NetShare.dmg
